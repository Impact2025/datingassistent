# =============================================================================
# VERCEL PRODUCTION DEPLOYMENT - Environment Variables Checklist
# =============================================================================
# Copy deze variabelen naar Vercel Dashboard > Settings > Environment Variables
# Vul alle [PLACEHOLDERS] in met je echte productie waarden
# =============================================================================

# DATABASE - Neon PostgreSQL (van: https://console.neon.tech/)
POSTGRES_URL=postgresql://[USER]:[PASS]@[HOST]/[DB]?sslmode=require&pgbouncer=true&connect_timeout=15
POSTGRES_PRISMA_URL=postgresql://[USER]:[PASS]@[HOST]/[DB]?sslmode=require&channel_binding=require&pgbouncer=true&connect_timeout=15
POSTGRES_URL_NON_POOLING=postgresql://[USER]:[PASS]@[HOST]/[DB]?sslmode=require&channel_binding=require

# AUTHENTICATION - Genereer nieuw: openssl rand -base64 32
JWT_SECRET=[GENERATE_NEW_32CHAR_SECRET]

# EMAIL - SendGrid API Key (van: https://app.sendgrid.com/settings/api_keys)
SENDGRID_API_KEY=SG.[YOUR_SENDGRID_API_KEY]

# AI - OpenRouter API Key (van: https://openrouter.ai/keys)
OPENROUTER_API_KEY=sk-or-v1-[YOUR_OPENROUTER_KEY]
NEXT_PUBLIC_APP_URL=https://[your-domain].vercel.app

# PAYMENT - MultiSafePay (van: https://merchant.multisafepay.com/)
MULTISAFEPAY_API_KEY=[YOUR_PRODUCTION_KEY]
NEXT_PUBLIC_MSP_TEST_MODE=false
MULTISAFEPAY_WEBHOOK_SECRET=[GENERATE_NEW_SECRET]

# APPLICATION
NEXT_PUBLIC_BASE_URL=https://[your-domain].vercel.app
NODE_ENV=production

# SECURITY - reCAPTCHA v3 (van: https://www.google.com/recaptcha/admin)
RECAPTCHA_SECRET_KEY=[YOUR_SECRET_KEY]
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=[YOUR_SITE_KEY]

# MONITORING - Sentry (van: https://sentry.io/settings/)
SENTRY_DSN=https://[KEY]@[REGION].ingest.sentry.io/[ID]
NEXT_PUBLIC_SENTRY_DSN=https://[KEY]@[REGION].ingest.sentry.io/[ID]
SENTRY_ORG=[your-org-slug]
SENTRY_PROJECT=[your-project-name]

# ANALYTICS - Google Analytics 4 (van: https://analytics.google.com/)
GA4_PROPERTY_ID=G-[YOUR_GA4_ID]
