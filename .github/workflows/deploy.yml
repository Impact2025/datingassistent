# Vercel handles deployments automatically via GitHub integration
# This workflow is no longer needed

name: Deploy to Vercel (Deprecated)

on:
  workflow_dispatch:  # Only manual trigger, not automatic

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Deployment is handled automatically by Vercel GitHub integration."
