# This workflow has been replaced by ci.yml
# Keeping this file to prevent the old workflow from running

name: CI/CD Pipeline (Deprecated)

on:
  workflow_dispatch:  # Only manual trigger, not automatic

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow has been deprecated. Use the CI workflow instead."
